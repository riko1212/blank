'use strict';

export const add = (a, b) => {
  return a + b;
};

export const sub = (a, b) => {
  return a - b;
};

export const division = (a, b) => {
  return a / b;
};

export const multi = (a, b) => {
  return a * b;
};

// export const math = {
//   add,
//   sub,
//   division
// }

export default {
  add,
  sub,
  division,
};

// export default 10;
